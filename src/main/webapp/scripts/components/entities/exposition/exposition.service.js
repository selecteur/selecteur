'use strict';

angular.module('selecteurApp')
    .factory('Exposition', function ($resource) {
        return $resource('api/expositions/:id', {}, {
            'query': { method: 'GET', isArray: true},
            'get': {
                method: 'GET',
                transformResponse: function (data) {
                    data = angular.fromJson(data);
                    return data;
                }
            },
            'update': { method:'PUT' }
        });
    });
